{% extends 'base.html.twig' %}

{% block title %}GSB{% endblock %}

{% block header %}
    <header class="masthead">
        <div class="container px-4 px-lg-5 h-100">
            <div class="row gx-4 gx-lg-5 h-100 align-items-center justify-content-center text-center">
                <div class="col-lg-8 align-self-end">
                    <h1 class="text-white font-weight-bold">Les Utilisateurs secondaires des fiches de frais</h1>
                    <hr />
                </div>
                {% if secondaire is empty %}
                <div class="col-lg-8 align-self-baseline">
                    <p class="text-white-75 mb-5">Aucun utilisateur secondaire n'est enregistré sur le site</p>
                </div>
                {% else %}
                <div class="col-lg-8 align-self-baseline">
                    <p class="text-white-75 mb-5">Liste des utilisateurs secondaires des fiches frais</p>
                </div>
                {% endif %}
            </div>
        </div>
    </header>
{% endblock %}
{% block section %}
    {% if secondaire is empty %}
        
    {% else %}    
    <section class="page-section" id="services">
        <div class="container px-4 px-lg-5">
            <h2 class="text-center mt-0">Les Utilisateurs Secondaires</h2>
            <hr class="divider" />
            <div class="row gx-4 gx-lg-5">
                <div class="page-content container">
                    <div class="row">
                    <div class="col-1"></div>
                        <div class="col-12 text-left">
                            <div class="block" style="background-color: #AAA">
                                <div class="container my-5 d-grid gap-5 ">
                                    <div class="p-5">
                                        <table class="table text-dark text-center">
                                            <thead>
                                              <tr>
                                                  
                                                <th scope="col">Fiche Frais</th>
                                                <th scope="col">Utilisateur Principal</th>
                                                <th scope="col">Utilsateur Secondaire</th>
                                                <th scope="col">Niveau</th>
                                                
                                              </tr>
                                            </thead>
                                            <tbody>
                                                
                                                {% for fiche in secondaire %}
                                              <tr>

                                                <td>{{ fiche.laFichefrais.id }}</td>
                                                <td>{{ fiche.laFichefrais.idutilisateur.prenom }}</td>
                                                <td>{{ fiche.Utili.prenom }} </td>
                                                <td>{{ fiche.niveau }} </td>
                                              </tr>
                                              {% endfor %}
                                          
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>   
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {% endif %}
{% endblock %}
